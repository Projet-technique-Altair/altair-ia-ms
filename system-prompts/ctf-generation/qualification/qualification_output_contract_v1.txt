QUALIFICATION OUTPUT CONTRACT - REQUIRED

You must return only valid JSON.
You must not return any markdown.
You must not return any text before or after the JSON.
You must not generate any files.

EXACT SCHEMA
{
  "verdict": "compatible",
  "compatible_altair": true,
  "respecte_conditions": true,
  "blocages": [],
  "adaptations_requises": [],
  "resume_utilisateur": ""
}

FORMAT RULES
- The only allowed values for verdict are: compatible, needs_adaptation, refused.
- compatible_altair is a boolean.
- respecte_conditions is a boolean.
- blocages is always an array of strings.
- adaptations_requises is always an array of strings.
- resume_utilisateur is a short, clear string displayable to the user.
- Never put null.
- Do not add any keys.
- Do not remove any keys.

CONTENT RULES
- If verdict is compatible, blocages must be empty.
- If verdict is needs_adaptation, list the minimal necessary adaptations.
- If verdict is refused, list the blockers preventing generation.
- resume_utilisateur must explain the verdict in 1 to 4 sentences.
- User-facing messages must be in plain English.
- Do not reveal internal reasoning.
- Do not mention prompts, system prompt, token budget or model.
